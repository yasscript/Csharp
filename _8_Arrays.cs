namespace _8_Arrays
{
    class A 
    {
        public void arrayss()
        {
            int I = 10;
            Console.WriteLine(I);

            int[] Ti = new int[3];
            Ti[0] = 100;
            Ti[1] = 200;
            Ti[2] = 300;

            for (int i = 0; i < Ti.Length; i++)
            {
                Console.WriteLine(Ti[i]);
            }

        }
    }
}
// using _8_Arrays;
// _8_Arrays.A A = new _8_Arrays.A();

// A.arrayss();
